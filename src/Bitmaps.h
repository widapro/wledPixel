#pragma once

#include <Arduino.h>
#include <MD_MAX72xx.h>

// ─── Pacman
// ────────────────────────────────────────────────────────────────────
extern const uint8_t F_PMAN1;
extern const uint8_t W_PMAN1;
extern const uint8_t PROGMEM pacman1[];

extern const uint8_t F_PMAN2;
extern const uint8_t W_PMAN2;
extern const uint8_t PROGMEM pacman2[];

// ─── Wave
// ──────────────────────────────────────────────────────────────────────
extern const uint8_t F_WAVE;
extern const uint8_t W_WAVE;
extern const uint8_t PROGMEM wave[];

// ─── Mario
// ─────────────────────────────────────────────────────────────────────
extern const uint8_t F_MARIO;
extern const uint8_t W_MARIO;
extern const uint8_t PROGMEM mario[];

// ─── Ghost
// ─────────────────────────────────────────────────────────────────────
extern const uint8_t F_GHOST;
extern const uint8_t W_GHOST;
extern const uint8_t PROGMEM ghost[];

// ─── Dino
// ──────────────────────────────────────────────────────────────────────
extern const uint8_t F_DINO;
extern const uint8_t W_DINO;
extern const uint8_t PROGMEM dino[];

// ─── Roll
// ──────────────────────────────────────────────────────────────────────
extern const uint8_t F_ROLL1;
extern const uint8_t W_ROLL1;
extern const uint8_t PROGMEM roll1[];

extern const uint8_t F_ROLL2;
extern const uint8_t W_ROLL2;
extern const uint8_t PROGMEM roll2[];

// ─── Lines
// ─────────────────────────────────────────────────────────────────────
extern const uint8_t F_LINES;
extern const uint8_t W_LINES;
extern const uint8_t PROGMEM lines[];

// ─── Arrow
// ─────────────────────────────────────────────────────────────────────
extern const uint8_t F_ARROW1;
extern const uint8_t W_ARROW1;
extern const uint8_t PROGMEM arrow1[];

extern const uint8_t F_ARROW2;
extern const uint8_t W_ARROW2;
extern const uint8_t PROGMEM arrow2[];

// ─── Sailboat
// ──────────────────────────────────────────────────────────────────
extern const uint8_t F_SAILBOAT;
extern const uint8_t W_SAILBOAT;
extern const uint8_t PROGMEM sailboat[];

// ─── Steamboat
// ─────────────────────────────────────────────────────────────────
extern const uint8_t F_STEAMBOAT;
extern const uint8_t W_STEAMBOAT;
extern const uint8_t PROGMEM steamboat[];

// ─── Heart
// ─────────────────────────────────────────────────────────────────────
extern const uint8_t F_HEART;
extern const uint8_t W_HEART;
extern const uint8_t PROGMEM heart[];

// ─── Invader
// ───────────────────────────────────────────────────────────────────
extern const uint8_t F_INVADER;
extern const uint8_t W_INVADER;
extern const uint8_t PROGMEM invader[];

// ─── Rocket
// ────────────────────────────────────────────────────────────────────
extern const uint8_t F_ROCKET;
extern const uint8_t W_ROCKET;
extern const uint8_t PROGMEM rocket[];

// ─── Fireball
// ──────────────────────────────────────────────────────────────────
extern const uint8_t F_FBALL;
extern const uint8_t W_FBALL;
extern const uint8_t PROGMEM fireball[];

// ─── Chevron
// ───────────────────────────────────────────────────────────────────
extern const uint8_t F_CHEVRON;
extern const uint8_t W_CHEVRON;
extern const uint8_t PROGMEM chevron[];

// ─── Walker
// ────────────────────────────────────────────────────────────────────
extern const uint8_t F_WALKER;
extern const uint8_t W_WALKER;
extern const uint8_t PROGMEM walker[];
